# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# Env
.env
.env.local

# IDE / OS
.DS_Store
*.code-workspace
.idea/
.vscode/
*.tmp
*.lock
!requirements.lock

# Outputs (generated files)
outputs/
out/

# OCR LLM 응답 캐시 — 재생성 가능한 로컬 산출물(회사별 수백 MB). corp_codes.json은 추적 유지.
data/_cache/ocr/

# Lock files from LibreOffice / other editors
.~lock.*

# Large doc files (proposals, PDFs in root — kept in project folder, not repo)
ESGenie_도전제안서_*.docx
ESGenie_도전제안서_*.pdf

# pytest cache
.pytest_cache/

# 시연 증빙 세트 (실제 문서/PDF — 저장소 미포함)
시연증빙세트_*/

CLAUDE.md
.claude/settings.local.json

# 실보고서 배치 검증용 원본 PDF — 제3자 저작물, 커밋 금지 (manifest만 추적)
data/real_reports/*.pdf
