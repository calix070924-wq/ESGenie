# ESGenie v15 — 의존성
# 설치: pip install -r requirements.txt --break-system-packages

# ── 기본 ──────────────────────────────────────────────────────────────
streamlit>=1.32
openpyxl>=3.1
python-dotenv>=1.0.0
requests>=2.31.0
pandas>=2.1.0
numpy>=1.26.0

# ── LLM / VLM ─────────────────────────────────────────────────────────
openai>=1.30.0                  # GPT-4o Vision (비정형 VLM 채널) + LLM 후처리

# ── OCR / PDF 렌더 ────────────────────────────────────────────────────
pymupdf>=1.23.0                 # PDF → 텍스트/이미지 (_quick_preview, _render_pages_b64)
pdf2image>=1.17.0               # pymupdf 없을 때 폴백
pillow>=10.0.0                  # pdf2image 의존

# ── 벡터 검색 / 임베딩 (v10 공유) ────────────────────────────────────
faiss-cpu>=1.7.4
sentence-transformers>=2.6.0

# ── 시각화 ────────────────────────────────────────────────────────────
plotly>=5.20.0

# ── 테스트 ────────────────────────────────────────────────────────────
pytest>=9.0.0

# ── 참고: CLOVA OCR ───────────────────────────────────────────────────
# 별도 SDK 없음 — requests로 REST API 직접 호출 (ocr_router._call_clova_ocr)
# 환경변수: CLOVA_OCR_SECRET, CLOVA_OCR_URL
# 키 없으면 자동으로 Mock 폴백 (데모 모드)
