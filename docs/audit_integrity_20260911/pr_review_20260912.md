# 감사 정합성 개선 PR 재검토 — 2026-09-12

전달본의 테스트 1,220개 통과를 재현했지만, 메인 화면과 실제 비DART 입력 경로에서 누락된 결함을 확인했다. 아래 보완 후 로컬 전체 1,224개와 개인 데이터가 없는 복사본의 1,212개가 통과했다. 검토 중 확인한 병합 차단 결함은 수정했으며, 표 검사와 추론 연도에 관한 기존 한계는 남는다.

## 기준과 대조

- 기준 main: `d3a06e22f41c5c2b0fb4fa4ad61cbe7d089cc2e6`. 재검토 때 원격 fetch로 동일함을 확인했다.
- 전달 브랜치 HEAD: `cdebcacae42056f6c219ffeb82b2e07e624e6baf`.
- 전달된 `review.patch`는 위 두 커밋의 Git diff와 바이트 단위로 일치한다. SHA-256: `2fd0ce505a9e4d9b6fa6d8dc5acb1e0280e7273f15e23b5a5b58ad606369da53`.
- 검토·보완은 기존 별도 worktree와 `codex/fix-audit-integrity-20260911`에서 수행했다. 원본 작업 폴더의 467개 파일과 `.env` 해시는 검토 전후 동일하다.

## 재현한 결함과 수정

| 우선순위 | 재현과 영향 | 수정·검증 |
|---|---|---|
| P1 | `final_score=None`을 메인 요약 카드에서 `.1f`로 표시하면 `TypeError`로 앱이 중단된다. 그 부분을 수정한 뒤 제출 보고서의 `overall_risk=None` 표시에서도 같은 오류를 확인했다. 기존 검증은 개별 탭 중심이라 메인 앱 전체의 이 상태를 놓쳤다. | 메인 카드·보고서 미리보기·관련 검증 스크립트가 공통 평가불가 표시 또는 JSON null을 사용한다. 실제 `app.py`를 실행하는 AppTest에서 일반·전문가 모드 모두 확인했다. |
| P1 | 설문 TextNode를 RAG에서 제외해도, 비DART 파이프라인이 이를 `CompanyReport.raw_text_snippets`에 복사해 `dart_raw` 청크로 재유입시킨다. 자가응답을 공시 원문으로 표시하는 출처 오류다. | 보고서 원문을 만들 때부터 설문을 제외하고 숫자 청크의 구버전 설문 출처도 배제한다. 예/아니오 모두 원장의 자가응답은 보존하고 독립 검색 인덱스는 비어 있는지 실제 파이프라인으로 검사했다. |
| P2 | 새 checkout에는 `outputs/`가 없다. 감사 캐시 테스트 8개가 통과한 뒤 결과 저장 hook에서 `FileNotFoundError`로 프로세스가 실패한다. | 결과 폴더를 생성한 뒤 저장한다. `git archive`로 만든 새 복사본에서 동일 8개 검사를 종료코드 0으로 재실행했다. |
| P2 | 시연 입력 목록 테스트가 Git에서 제외된 개인 PDF 폴더를 요구해 새 checkout에서 20개·7개 입력 검사가 실패한다. | 파일 내용과 무관한 목록·역할 검사는 임시 폴더의 합성 파일명으로 실행한다. 20개/7개, 객관 증빙 17개/4개, SAQ 3개라는 기대값은 유지했다. |

추가 회귀 4개는 수정 전 모두 실패했다. 메인 카드 수정 후 보고서 탭에서 이어진 실패도 확인하고 수정했다. 모델이 없는 환경에서는 기존 검색 게이트의 폴백 우회 설정이 달라지므로 새 출처 회귀는 운영용 검색 차단을 명시적으로 켜서 검사한다. 제품의 폴백 정책이나 임계값은 바꾸지 않았다.

기준 로그/XML의 의미 없는 줄 끝 공백 4곳을 정리해 diff 검사를 통과시켰다. 기존 실패 내용과 결과는 보존했다.

## 재검증 결과

| 범위 | 결과 |
|---|---|
| 전달본 전체 테스트 재실행 | 1,220 통과 / 0 실패 / 0 skip, 43.32초 |
| 수정 후 로컬 전체 | 1,224 통과 / 0 실패 / 0 skip, 43.22초 |
| 최종 테스트 fixture 변경 확인 | 관련 12개 통과 |
| 개인 PDF·`.env`·임베딩 캐시 없는 복사본 | 1,212 통과 / 0 실패 / 12 skip, 7.58초 |
| 새 checkout에서 감사 캐시 검사 단독 실행 | 8개 통과, 정상 종료 |
| Git diff·구문 검사 | 통과 |
| 기존 커밋 111개 신규 blob 및 보완 파일의 실제 자격증명 값 대조 | 노출 없음 |

복사본의 12 skip은 기존 `test_table_structure.py`가 원본 실보고서 PDF를 찾지 못할 때 수행하는 검사 생략이다. skip/xfail을 추가하지 않았다. 같은 검사 12개는 원본 PDF가 있는 로컬 전체 실행에서 통과했다. 최종 JUnit 형식을 `xunit1`로 설정해 관측 속성과 형식을 일치시켰으며 남은 경고 5개는 PyMuPDF/SWIG deprecation이다.

GitHub Actions는 새 checkout에서 감사 캐시 검사와 전체 회귀를 실행하고 JUnit XML·감사 관측 JSON을 저장한다. API 키를 사용하지 않고, 테스트 중 외부 소켓 및 모델 다운로드를 차단한다. GitHub 실행의 실제 결과는 PR Checks와 해당 실행의 artifact에서 확인한다.

## 기존 실제 API 자료 대조

이번 재검토에서 새 DP/GPT 호출을 하지 않았다. 9월 12일 전달된 실제 응답·캐시 재사용 기록을 읽어 대조했다.

- `final`과 `final_reuse`의 입력·설정·원장·위험도 표·주장·응답·영역별 결과가 모두 같다.
- `final`: DP 3회 모두 HTTP 200, 모델 `document-parse-260630`; GPT 실제 성공 4회, LLM 캐시 15회 적중. 반환 모델은 `gpt-4.1-mini-2025-04-14`.
- `final_reuse`: GPT 실제 호출 0회, LLM 캐시 19회·비정형 OCR 캐시 1회 적중. 실패 목록은 비어 있다.
- 확정값은 에너지 0.513216 TJ, 폐기물 18.4톤, 재활용률 29.3%, 파생 배출량 88.397 tCO2eq다. 모두 2026년이며 추정·부분값 관련 상태를 보존한다.
- RBA `RBA-C-4-E-6-2`는 29.3%, 2026년, 검토필요다. 저장 결과의 E 점수 7.46, S 0이지만 사람 검토, G null·사람 검토도 보고서와 일치한다.

## 남은 한계

- 실제 저장본에서 전기요금·폐기물 문서의 표 게이트는 `ESCALATE`, pending=true다. 도시가스 문서는 `ACCEPT`, pending=false다. DP HTTP 성공과 표 검사 통과는 다르다.
- 추론 연도·월/공장 부분값·배출계수에 의한 파생 배출량을 연간 실측 확정값으로 확대 해석할 수 없다. SAQ 수치 옆에 연도가 없으면 주장 연도는 null이다.
- 개인 데이터가 없는 CI는 해시 임베딩 폴백을 사용한다. 이는 설치·회귀 검증이며 실제 SBERT 또는 외부 API 품질 검증을 대신하지 않는다.
- 새 회귀는 실제 Streamlit 스크립트 실행을 검사한다. 브라우저 화면 캡처나 새 실제 기업 문서에 대한 일반화 성능 검증은 수행하지 않았다.

세부 수치는 `pr-review-results.json`, 최초 검증과 실제 API 내역은 `result.md`와 `live-evidence.json`을 함께 참조한다. 기존 전달본을 덮어쓰지 않았으며 main 병합은 수행하지 않았다.
