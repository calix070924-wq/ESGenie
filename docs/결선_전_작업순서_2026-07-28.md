# 결선 전 작업 순서 (2026-07-28)

전수 분석 결과를 실행 순서로 재배열. 순서 기준 = **(심사 노출 위험 × 발각 시 손상) ÷ 작업량**.
분석 원문 대비 3건은 재확인 결과가 달라 아래에 반영함(§0).

---

## 0. 분석 원문에서 수정된 사실

| 원문 주장 | 재확인 결과 |
|---|---|
| 미머지 브랜치 스택 4단 | d1-topic-match·ocr-cache·l1-node-selection은 **이미 origin/main 머지 완료**(PR #45~#47). 남은 건 현재 브랜치 `feature/gate-false-block` 3커밋, **원격 브랜치 없음(로컬 전용)** |
| S-3-1 오배치 = prompts.py 문제 | 범위가 더 넓음. `dart_client.py:607-608,650`이 **산재율/재해율을 S-3-1로 파싱** 중인데 `knowledge/kesg_items.py:94`는 `S-3-1 = 여성 구성원 비율`. 규정 체크리스트 오배치와 **동일 뿌리**이며 데이터 오염까지 이어짐 |
| D6 signal_a만 불일치 | signal_b도 README에 없는 `min(1, orphan_weight/pairs × 2.0)` 정규화 사용(`layer3_disclosure.py:192`). 점수식 문단 전체를 다시 써야 함 |

---

## 1순위 — README 수치 정합화 ✅ 2026-07-28 완료 (코드 변경 0)

가장 위험하고 가장 쌌습니다. 심사위원 첫 질문이 여기서 나옵니다.

- [x] `README.md:8` — `codex -p azure` 붙여넣기 잔여물 삭제
- [x] held-out 블록 — test F1 `0.842` → `0.522(CI 0.404–0.629, n=270)`, Precision 0.938 / Recall 0.361 추가, 특이도 `0.978(46문장)` → `0.989(187문장 중 185건, CI 0.973–1.000)`, 교차도메인 recall 0.393 명시
- [x] D6 벤치 — `12케이스 100%` → `15케이스(설계 12 + 실보고서 3) 레벨 80.0% / 이진 86.7%`, 미탐 3건(D6-08·09·10) 원인까지 기재
- [x] D6 점수식 — `0.60×A + 0.45×B, A=절대 누적식` → 실제 코드식 `min(1, 0.45×A + 0.55×B)`, A·B 정규화 방식 명시
- [x] "dev 1.000 → test 0.522 격차 자체가 held-out 분리의 산출물" 프레이밍 1문단 추가

**검증 완료**: 낡은 수치 grep 잔존 0건(133행 "12케이스"는 이력 서술로 의도적 존치) + `python -m esgenie.benchmark_disclosure` 재실행 → 15케이스·레벨 80.0%·이진 86.7%·불일치 D6-08/09/10 실측 일치.

**작업 중 확정된 사실**: `git log -S`로 확인한 결과 `layer3_disclosure.py`의 signal_a는 **2026-06-13 최초 커밋 이후 한 번도 바뀌지 않았다**. README의 "2026-06-14 signal_a 재조정" 문구는 2026-06-18 커밋(c00fcec)에 들어갔지만 **코드에는 반영된 적이 없다** — README가 미구현 계획을 완료형으로 기술 중이었다. 원 분석의 "README가 옳고 코드가 낡았다" 추정은 반대였다.

**근거**: `docs/held_out_eval_methodology.md` §2가 0.842를 "완전히 대체된 값"이라고 스스로 명시. 대외 문서만 낡아 있었음 → 이대로 노출되면 다른 정직한 문서까지 의심받는 구조였다.

---

## 2순위 — S-3-1 계열 코드 정합성 ✅ 2026-07-28 완료

데모 중 K-ESG를 아는 심사위원이 보면 3초 만에 잡히는 건이었습니다. 도메인 신뢰도 직결.

- [x] `prompts.py` POLICY_CHECKLISTS 키 재매핑 — 11개 중 9개 오배치 정정. 체크리스트 **내용은 한 줄도 건드리지 않고 키만 이동**(원 저자가 의도한 주제는 맞았고 코드만 틀렸음)
- [x] `audit_trace.py:203` `_kesg_name()` — 하드코딩 5개 표를 `kesg_items.ALL_ITEMS` 단일 출처 조회로 교체. 표 자체가 재발 원인이었음
- [x] `dart_client.py` 산재율/재해율 정규식 `S-3-1` → **`S-4-2`(산업재해율)**
- [x] `dart_client.py` sanity bound — `("S-3-1","%") = (0,10)`이 재해율 기준이라 **여성 구성원 비율(통상 10~40%)을 조용히 버리고 있었음**. `S-4-2 = (0,10)` / `S-3-1 = (0,100)`으로 분리
- [x] `pipeline.py` 데모 하한 하드코딩 `"S-3-1","E-1-1"` → `"S-4-1","E-1-1"`
- [x] `pipeline.py` POLICY_CODES에 `E-8-1` 추가 (재매핑으로 생긴 고아 체크리스트를 정합성 테스트가 잡아냄)
- [x] `tests/test_policy_checklist_integrity.py` 신규 6케이스
- [x] `llm.py:647` `_v("S-3-1", 24.8)` — 확인 결과 **원래 맞음**(S-3-1 = 여성 구성원 비율). 수정 불필요

**재매핑 표**

| 이동 전 | 체크리스트 주제 | 이동 후 | 이동 후 실제 항목명 |
|---|---|---|---|
| E-2-1 | 환경 법규 준수 | **E-8-1** | 환경 법규 위반 |
| E-3-1 | 온실가스 관리 | **E-3-3** | 온실가스 배출량 검증 |
| S-1-1 | 인권/노동 정책 | **S-5-1** | 인권정책 수립 |
| S-2-1 | 공정거래·동반성장 | **S-6-1** | 협력사 ESG 경영 |
| S-3-1 | 안전보건 시스템 | **S-4-1** | 안전보건 추진체계 |
| S-4-1 | 정보보호 정책 | **S-8-1** | 정보보호 시스템 구축 |
| G-3-1 | 윤리경영·부패방지 | **G-4-1** | 윤리규범 위반사항 공시 |
| E-1-1·E-1-2 | (원래 정합) | 유지 | — |
| G-1-1·P-1-1 | 코드는 맞음 | 유지 | 주석만 실제 항목명으로 정정 |

**검증 완료**: 전체 스위트 **818 passed / 0 failed**(샌드박스는 streamlit·plotly 미설치라 UI 테스트 2모듈 제외 — 맥에서 재실행 필요). 규정 검증 커버리지 **7/15 → 11/16**, 그중 **잘못된 항목을 검사하던 4건이 제거**됨. `_kesg_name("S-3-1")` → `여성 구성원 비율` 확인.

**남은 결정 (사용자 판단 필요)**

1. **체크리스트 미보유 5건** — `S-1-1`(목표 수립 및 공시) · `S-2-6`(결사의 자유 보장) · `S-7-1`(전략적 사회공헌) · `G-3-1`(주주총회 소집 공고) · `G-5-1`(내부감사부서 설치). 현재도 `detector_5axis`의 no-checklist 조기 반환으로 **무조건 `passed=True`** → 통과율 분모를 부풀림. 채울지 / 분모에서 뺄지 결정 필요. 현황은 테스트로 고정해뒀으니 늘어나면 실패함
2. **G-1-1 / P-1-1** — 코드는 POLICY_CODES와 맞지만 체크리스트 내용이 항목명보다 넓음(G-1-1 "이사회 내 ESG 안건 상정"인데 내용은 사외이사 비율·겸직 제한까지, P-1-1 "ESG 정보공시 방식"인데 내용은 추진체계). 내용을 좁힐지, 항목을 바꿀지는 도메인 판단

---

## 3순위 — D3 중립값 0.5 제거 (예상 2~3h)

화면 결함(리스크 표 13행 전부 0.125)과 일반화 성능(ASA 신규 16건 미탐)이 **같은 상수 하나**에서 나옵니다. 하나 고치면 둘 다.

- [ ] `layer3_detect.py:571` 청크 없을 때 `0.5` → `None`(미산정)
- [ ] 종합 위험도 계산에서 D3 미산정 시 **가중치 재정규화**(0.40/0.25/-/0.10 → 합 1.0)
- [ ] UI(`ui/tabs.py:1092`)에 "D3 미산정" 배지 표기 — 숨기지 말고 보이게(이 프로젝트 기존 폴백 설계 원칙과 일치)

**검증**: 라이브 재실행해 risk_rows 값이 분산되는지 + held-out ASA 16건 재측정.

**주의**: 종합점수 분포가 바뀌므로 threshold 0.25 재확인 필요. 회귀 위험 있는 유일한 상위 항목이라 1·2순위 뒤로 뒀습니다.

---

## 4순위 — D6 점수식 결정 (예상 2h)

> **조사 완료(1순위 작업 중)**: 코드가 원본이고 README가 미구현 계획을 적어둔 것이었음. 문서 쪽은 1순위에서 코드에 맞춰 정정했으므로, 남은 건 순수한 설계 결정 — **비율식을 유지할지, 절대 누적식을 실제로 구현할지**.

- [ ] signal_a를 `min(1, Σ민감가중 / 4.0)` 절대 누적식으로 **구현**할지 결정. 구현 시 D6-08(0.231 low → high)·D6-09·D6-10 3건 미탐이 해소될 가능성이 높음
- [ ] 구현하면 low 6건(현재 전건 정답)이 위로 밀려 오탐이 생길 수 있으므로 **15케이스 전체 재캘리브레이션 필수** — `_LEVELS` 임계값도 함께 재산정
- [ ] 실보고서 3건(D6-REAL-01~03)이 현재 전건 정답이므로, 재캘리브레이션 후에도 유지되는지가 채택 기준
- [ ] 확정 시 README 점수식 문단(1순위에서 코드 기준으로 써둠) 재갱신

**차별점 축의 핵심 케이스라 미탐이 남으면 D6 셀링포인트 자체가 약해집니다.** 다만 지금은 README가 미탐 3건을 원인까지 밝혀 적고 있어, 고치지 않아도 "숨겼다"는 비판은 받지 않습니다.

---

## 5순위 — legacy vs 4축 관계 명문화 (예상 30분, 코드 0)

"종합 위험도 34.1이 4축 가중합인가요?" → **아니오**. 준비 안 하면 치명적, 준비하면 무해.

- [ ] README에 한 문단: *"화면·보고서 헤드라인의 0–100 점수는 legacy 합성점수(numeric_mismatch 0.45 / vague_language 0.25 / semantic_gap 0.20 / unverifiable 0.10). D1~D5 4축 RiskVector는 L4 재작성 제약 도출에 사용."*
- [ ] 발표 Q&A 스크립트에 동일 답변 1문장 포함

**S영역이 legacy LOW vs 4축 0.04로 순위까지 다른 케이스가 있으니, "둘이 같은 걸 다르게 표현한 것"이라고 답하면 안 됩니다.**

---

## 6순위 — main 최신화 (예상 30분)

- [ ] `feature/gate-false-block` (로컬 3커밋, **원격 없음**) push → PR → main 머지
- [ ] README 수정분(1순위)도 함께 커밋 — 현재 작업트리에 미커밋 상태
- [ ] 나머지 브랜치는 이미 머지 완료 — 추가 작업 없음
- [ ] 머지 후 `pytest` 전체 재실행(현재 기준선 834)

---

## 7순위 — 제출물 정리 (예상 1h, 판단 필요)

- [ ] **업종 모듈 결정**: `industry_module 5개사 전부 null` 기록이 있는 상태에서 업종 특화를 셀링포인트로 쓰면 자기부정. **주장을 빼거나 / 버그를 고치거나** 하나 선택
- [ ] `docs/evidence_recall.md`(mock 모드 recall 1.000) — 제출물에서 제외하거나 파일명에 `_mock` 명시
- [ ] L4 재생성 데모 화면에 mock 경고 노출(`benchmark.py:assess_run_mode` 수준의 정직함을 UI에도)

---

## 결선 후로 미룸 (회귀 위험 대비 이득 낮음)

- D2 온/오프 → 연속 스코어화 (clean 187건 중 28건 오탐, 단 precision 0.938과 얽혀 있어 단순치 않음)
- L4 G2_orphan_numbers 하드페일 → 수렴 실패 3/3 (+ `grounding_gate.py:83` claim 0건일 때 faithfulness 1.0 방향 역전)
- `layer0_evidence_graph.py` 700줄 정리 — 단순 사문화가 아니라 **벤치 경로와 라이브 경로가 다른 L0를 쓰는 구조 문제**라 시간 소요 큼
- `detector_5axis.detect_risk_axes`의 D_WEIGHTS 로컬 재구현 → 공통 상수 참조

---

## 최단 경로 요약

1·2순위 완료. 남은 최소 방어선은 **5순위(legacy vs 4축 한 문단, 30분)** 하나입니다 — 이것까지 끝내면 심사 리스크의 대부분(문서-코드 불일치 + 도메인 오류 + 방어 못 하는 질문)이 사라집니다.
3·4는 하루 더, 회귀 검증 시간 확보 가능할 때만.

**맥에서 해야 할 확인 2건**: ① UI 테스트 2모듈(`test_ui_tabs`·`test_ui_apptest_smoke`) 포함 전체 스위트 재실행 ② 라이브 1회 돌려 규정 검증 탭에 `S-4-1 안전보건 추진체계`로 뜨는지 육안 확인.
